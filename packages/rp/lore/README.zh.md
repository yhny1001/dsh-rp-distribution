# @dsh-rp/lore

[English](README.md) | 中文

`ctx.rpLore` 按作用域注册世界书，并通过有边界、确定性的字面匹配激活已启用条目。

## 模型体验

通过为模型请求选择已激活世界事实的 Prompt 消费者间接产生影响。

#### KV Cache 影响

激活的 Lore 属于动态上下文；确定性排序可避免意外的缓存抖动。

## 已知限制与延期工作

- Regex、向量、递归、概率、冷却和 Decorator 语义必须由显式插件提供。
