import{ describe,expect,it }from'vitest';import{ Context }from'@deepseek-ai/cordis';import RpPromptRuntime from'../src/index.ts';describe('@dsh-rp/prompt',()=>{it('orders constraints before priorities and rejects cycles',async()=>{const ctx=new Context();await ctx.plugin(RpPromptRuntime);const scope={ kind:'turn' as const,id:'t' };ctx.rpPrompt.register(scope,{ schemaVersion:1,id:'actor',role:'system',content:'Actor',priority:20,after:['world'] });ctx.rpPrompt.register(scope,{ schemaVersion:1,id:'world',role:'system',content:'World',priority:100 });expect(ctx.rpPrompt.compose(scope).sections.map(section=>section.id)).toEqual(['world','actor']);expect(()=>ctx.rpPrompt.compose(scope,[{ schemaVersion:1,id:'x',role:'system',content:'x',priority:0,after:['missing'] }])).toThrow(/missing/u)})})
